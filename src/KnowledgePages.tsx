import {useEffect,useMemo,useState} from 'react';
import type {ApiClient} from './api';import type {KnowledgeBase,KnowledgeChunk,KnowledgeDocument,KnowledgeRetrievalLog,KnowledgeRetrievalResult,KnowledgeStatistics} from './types';import './knowledge-pages.css';
type Notify=(x:string,b?:boolean)=>void;
const defaults={name:'',description:'',type:'document',edition:'standard',embedding_model:'hashing',retrieval_mode:'hybrid',chunk_strategy:'smart',chunk_size:600,chunk_overlap:80,similarity_threshold:.15,top_k:5,rerank_enabled:false};
export function KnowledgeBases({api,notify}:{api:ApiClient;notify:Notify}){const [items,setItems]=useState<KnowledgeBase[]>([]),[loading,setLoading]=useState(true),[q,setQ]=useState(''),[status,setStatus]=useState(''),[wizard,setWizard]=useState(false),[active,setActive]=useState<KnowledgeBase|null>(null);const load=()=>{setLoading(true);api.get<KnowledgeBase[]>('/api/knowledge-bases').then(setItems).catch(e=>notify((e as Error).message,true)).finally(()=>setLoading(false))};useEffect(load,[api]);const list=useMemo(()=>items.filter(x=>(!q||x.name.toLowerCase().includes(q.toLowerCase()))&&(!status||x.status===status)),[items,q,status]);if(active)return <KnowledgeDetail api={api} notify={notify} kb={active} back={()=>{setActive(null);load()}}/>;return <main className="kb-page"><header className="kb-head"><div><h1>知识库</h1><p>集中导入、解析和检索企业资料，并挂载到智能体与工作流。</p></div><button className="violet-btn" onClick={()=>setWizard(true)}>＋ 创建知识库</button></header><section className="kb-toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索知识库名称"/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">全部状态</option><option value="ready">可用</option><option value="processing">处理中</option></select><span>标准版：本地文档与混合检索</span></section>{loading?<div className="kb-empty">正在加载知识库…</div>:list.length?<section className="kb-grid">{list.map(k=><article key={k.id}><header><b>{k.name}</b><span className={'kb-status '+k.status}>{k.status==='ready'?'可用':k.status}</span></header><p>{k.description||'暂无描述'}</p><dl><div><dt>文档</dt><dd>{k.document_count}</dd></div><div><dt>切片</dt><dd>{k.chunk_count}</dd></div><div><dt>检索</dt><dd>{k.retrieval_mode}</dd></div></dl><footer><small>{new Date(k.updated_at).toLocaleString('zh-CN')}</small><button onClick={()=>navigator.clipboard?.writeText(k.id).then(()=>notify('ID 已复制'))}>复制 ID</button><button onClick={()=>setActive(k)}>管理</button><button className="danger" onClick={async()=>{if(confirm(`删除知识库「${k.name}」？`)){try{await api.delete(`/api/knowledge-bases/${k.id}`);load();notify('知识库已删除')}catch(e){notify((e as Error).message,true)}}}}>删除</button></footer></article>)}</section>:<div className="kb-empty"><b>还没有知识库</b><p>创建后可上传文件并在应用中挂载。</p></div>}{wizard&&<KnowledgeWizard api={api} notify={notify} close={()=>setWizard(false)} done={k=>{setWizard(false);setActive(k)}}/>}</main>}
function KnowledgeWizard({api,notify,close,done}:{api:ApiClient;notify:Notify;close:()=>void;done:(k:KnowledgeBase)=>void}){const [step,setStep]=useState(1),[data,setData]=useState<any>(defaults),[saving,setSaving]=useState(false);const change=(k:string,v:any)=>setData((x:any)=>({...x,[k]:v}));const finish=async()=>{setSaving(true);try{done(await api.post<KnowledgeBase>('/api/knowledge-bases',data));notify('知识库已创建')}catch(e){notify((e as Error).message,true)}finally{setSaving(false)}};return <div className="overlay"><section className="kb-modal"><header><h2>创建知识库</h2><button onClick={close}>×</button></header><ol className="kb-steps">{['基础信息','数据来源','索引与检索','确认创建'].map((x,i)=><li className={step===i+1?'active':''} key={x}>{i+1}. {x}</li>)}</ol><div className="kb-form">{step===1&&<><label>知识库名称 *<input autoFocus value={data.name} onChange={e=>change('name',e.target.value)} placeholder="例如：员工手册"/></label><label>描述<textarea value={data.description} onChange={e=>change('description',e.target.value)}/></label><label>规格<select value={data.edition} onChange={e=>change('edition',e.target.value)}><option value="standard">标准版</option><option value="enterprise">旗舰版</option></select></label></>}{step===2&&<div className="source-guide"><b>创建后上传数据</b><p>支持 TXT、Markdown、HTML、PDF、DOCX、XLSX、CSV、JSON，单文件不超过 20MB。系统会在上传后自动解析、切片并建立索引。</p></div>}{step===3&&<><label>切片策略<select value={data.chunk_strategy} onChange={e=>change('chunk_strategy',e.target.value)}>{[['smart','智能切分（本地规则）'],['paragraph','按段落'],['heading','按标题'],['page','按页'],['fixed','固定长度'],['regex','自定义正则']].map(x=><option value={x[0]} key={x[0]}>{x[1]}</option>)}</select><small>不调用大模型：先按句末标点和换行断句，再按长度合并；超长文本按长度切开并保留重叠。</small></label><div className="two"><label>切片长度<input type="number" value={data.chunk_size} onChange={e=>change('chunk_size',+e.target.value)}/></label><label>重叠长度<input type="number" value={data.chunk_overlap} onChange={e=>change('chunk_overlap',+e.target.value)}/></label></div><div className="two"><label>检索模式<select value={data.retrieval_mode} onChange={e=>change('retrieval_mode',e.target.value)}><option value="hybrid">混合检索</option><option value="dense">向量检索</option><option value="sparse">关键词检索</option></select></label><label>Top-K<input type="number" value={data.top_k} onChange={e=>change('top_k',+e.target.value)}/></label></div></>}{step===4&&<div className="confirm"><b>{data.name||'未命名知识库'}</b><p>{data.description||'无描述'}</p><small>{data.chunk_strategy} · {data.retrieval_mode} · Top {data.top_k}</small></div>}</div><footer><button onClick={step===1?close:()=>setStep(step-1)}>取消/上一步</button>{step<4?<button className="primary" disabled={step===1&&!data.name.trim()} onClick={()=>setStep(step+1)}>下一步</button>:<button className="primary" disabled={!data.name.trim()||saving} onClick={finish}>{saving?'创建中…':'完成创建'}</button>}</footer></section></div>}
function KnowledgeDetail({api,notify,kb,back}:{api:ApiClient;notify:Notify;kb:KnowledgeBase;back:()=>void}){const [tab,setTab]=useState('overview'),[docs,setDocs]=useState<KnowledgeDocument[]>([]),[chunks,setChunks]=useState<KnowledgeChunk[]>([]),[stats,setStats]=useState<KnowledgeStatistics|null>(null),[logs,setLogs]=useState<KnowledgeRetrievalLog[]>([]),[query,setQuery]=useState(''),[results,setResults]=useState<KnowledgeRetrievalResult[]>([]),[busy,setBusy]=useState(false);const load=()=>Promise.all([api.get<KnowledgeDocument[]>(`/api/knowledge-bases/${kb.id}/documents`).then(setDocs),api.get<KnowledgeChunk[]>(`/api/knowledge-bases/${kb.id}/chunks`).then(setChunks),api.get<KnowledgeStatistics>(`/api/knowledge-bases/${kb.id}/statistics`).then(setStats),api.get<KnowledgeRetrievalLog[]>(`/api/knowledge-bases/${kb.id}/logs`).then(setLogs)]).catch(e=>notify((e as Error).message,true));useEffect(()=>{load()},[api,kb.id]);const upload=async(e:React.ChangeEvent<HTMLInputElement>)=>{const file=e.target.files?.[0];if(!file)return;setBusy(true);const form=new FormData();form.append('file',file);try{await api.postRaw(`/api/knowledge-bases/${kb.id}/documents`,form);notify('文件已上传并完成解析');load()}catch(x){notify((x as Error).message,true)}finally{setBusy(false);e.target.value=''}};const test=async()=>{try{const x=await api.post<any>(`/api/knowledge-bases/${kb.id}/retrieval-test`,{query,mode:kb.retrieval_mode,top_k:kb.top_k,threshold:kb.similarity_threshold});setResults(x.documents);load()}catch(e){notify((e as Error).message,true)}};return <main className="kb-page"><header className="kb-head"><div><button className="back" onClick={back}>← 返回</button><h1>{kb.name}</h1><p>{kb.description||'知识库详情与运行状态'}</p></div><span className="kb-status ready">可用</span></header><nav className="kb-tabs">{[['overview','概览'],['documents','数据管理'],['chunks','切片管理'],['settings','检索配置'],['test','命中测试'],['logs','调用日志']].map(x=><button className={tab===x[0]?'active':''} onClick={()=>setTab(x[0])} key={x[0]}>{x[1]}</button>)}</nav>{tab==='overview'&&<section className="kb-overview"><article><b>{stats?.document_count||0}</b><span>文档数量</span></article><article><b>{stats?.chunk_count||0}</b><span>有效切片</span></article><article><b>{stats?.retrieval_count||0}</b><span>检索调用</span></article><article><b>{stats?.parse_failed_count||0}</b><span>解析失败</span></article></section>}{tab==='documents'&&<section><label className="upload"><input type="file" accept=".txt,.md,.markdown,.html,.htm,.pdf,.docx,.xlsx,.csv,.json" onChange={upload}/>{busy?'处理中…':'上传文档'}</label><div className="kb-table">{docs.map(d=><article key={d.id}><b>{d.filename}</b><span>{d.parse_status} / {d.index_status}</span><small>{d.error_message||`${d.file_size} bytes`}</small><button onClick={async()=>{await api.post(`/api/knowledge-bases/${kb.id}/documents/${d.id}/reparse`);load()}}>重新解析</button><button className="danger" onClick={async()=>{if(confirm('删除文档及其切片？')){await api.delete(`/api/knowledge-bases/${kb.id}/documents/${d.id}`);load()}}}>删除</button></article>)}</div></section>}{tab==='chunks'&&<section className="kb-table">{chunks.map(c=><article key={c.id}><b>{c.title||'未命名切片'} #{c.chunk_index}</b><p>{c.content}</p><small>{c.token_count} tokens · {c.enabled?'启用':'禁用'}</small><button onClick={async()=>{const content=prompt('编辑切片内容',c.content);if(content!==null){await api.put(`/api/knowledge-bases/${kb.id}/chunks/${c.id}`,{content});load()}}}>编辑</button><button className="danger" onClick={async()=>{if(confirm('删除切片？')){await api.delete(`/api/knowledge-bases/${kb.id}/chunks/${c.id}`);load()}}}>删除</button></article>)}</section>}{tab==='settings'&&<section className="settings"><p>当前配置：{kb.retrieval_mode} · Top-K {kb.top_k} · 阈值 {kb.similarity_threshold} · {kb.chunk_strategy}</p><p>修改切片策略后，对文档执行“重新解析”以重新建立索引。</p></section>}{tab==='test'&&<section className="test"><div><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="输入问题，测试文档命中效果"/><button className="primary" disabled={!query.trim()} onClick={test}>开始检索</button></div>{results.map(r=><article key={r.chunk_id}><header><b>{r.filename}</b><span>得分 {r.score}</span></header><p>{r.content}</p></article>)}</section>}{tab==='logs'&&<section className="kb-table">{logs.map(l=><article key={l.id}><b>{l.query}</b><span>{l.result_count} 条结果</span><small>{l.latency_ms}ms · {new Date(l.created_at).toLocaleString('zh-CN')}</small></article>)}</section>}</main>}
